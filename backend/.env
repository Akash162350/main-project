# Server
PORT=3000
NODE_ENV=production

# AWS RDS MySQL connection details
# Get these from the RDS console -> your DB instance -> "Connectivity & security"
# and the master username/password you set when you created the instance.
DB_HOST=aws-devops-mysql.c29u6youizmg.us-east-1.rds.amazonaws.com
DB_PORT=3306
DB_USER=admin
DB_PASSWORD=~D0j.2i!jXJ.XSBvk0U87)y6YRI0
DB_NAME=aws-devops-mysql

# Optional: enable email notifications when someone submits the contact form
# Leave EMAIL_ENABLED=false if you don't want to configure SMTP right away.
EMAIL_ENABLED=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
NOTIFY_TO=akashgpb02@gmail.com

# Comma separated list of origins allowed to call the API.
# Set this to your real domain once it's live, e.g. https://akashgp.com
CORS_ORIGIN=*
